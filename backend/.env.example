# MongoDB: use a dedicated database name for this project to avoid impacting other apps on the cluster.
# The connection string may contain another db name (e.g. scopeshift); we override to diabevision in code.
MONGODB_URI=mongodb+srv://user:pass@cluster0.xxx.mongodb.net/?retryWrites=true&w=majority

# JWT secret for auth tokens (use a long random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server
PORT=4000

# Allowed frontend origin(s) for CORS (comma-separated for multiple)
CORS_ORIGIN=http://localhost:3000,https://diabevision.onrender.com
